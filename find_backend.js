const { Client } = require('ssh2');
const conn = new Client();
conn.on('ready', () => {
  conn.exec(`find /root -name "backend" -type d`, (err, stream) => {
    if (err) throw err;
    stream.on('close', (code, signal) => {
      conn.end();
    }).on('data', (data) => {
      console.log('STDOUT: ' + data.toString().trim());
    });
  });
}).connect({
  host: '135.125.9.81',
  port: 20064,
  username: 'root',
  password: 'SRhP8Rw_WJD8jZP2',
  readyTimeout: 15000
});
