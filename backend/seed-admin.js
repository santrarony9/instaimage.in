const { MongoClient } = require('mongodb'); const bcrypt = require('bcrypt'); async function seedAdmin() { const client = new MongoClient('mongodb://127.0.0.1:27017'); await client.connect(); const db = client.db('marketplace'); const users = db.collection('users'); const email = 'admin@instaimage.com'; const existing = await users.findOne({ email }); if (existing) { console.log('Admin already exists'); } else { const salt = await bcrypt.genSalt(10); const password = process.env.ADMIN_PASSWORD || 'ChangeThisPasswordImmediately!'; const hash = await bcrypt.hash(password, salt); await users.insertOne({ name: 'Super Admin', email, passwordHash: hash, role: 'ADMIN', isActive: true, createdAt: new Date(), updatedAt: new Date(), isDeleted: false }); console.log(`Admin created: admin@instaimage.com / ${password}`); } await client.close(); } seedAdmin();
